packages:
  - .
  - mcp/*

allowBuilds:
  # Native file watcher used by the Tailwind CLI's --watch mode (watch:css).
  '@parcel/watcher': true
  esbuild: true
  sharp: true

minimumReleaseAgeExclude:
  - lucide-static@1.35.0
